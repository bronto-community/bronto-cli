version: "2"
run:
  timeout: 5m
linters:
  exclusions:
    rules:
      - path: internal/api/gen\.go
        linters:
          - errcheck
          - staticcheck
          - unused
          - ineffassign
