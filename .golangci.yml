run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - ineffassign
issues:
  exclude-dirs: []
  exclude-rules:
    - path: internal/api/gen\.go
      linters: [errcheck, staticcheck, unused, ineffassign]
